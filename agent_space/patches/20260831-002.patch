diff --git a/agent_space/patches/agent-space-layout.patch b/agent_space/patches/20260831-001.patch
similarity index 100%
rename from agent_space/patches/agent-space-layout.patch
rename to agent_space/patches/20260831-001.patch
diff --git a/agent_space/patches/agent-space-layout.md b/agent_space/patches/20260831-001.md
similarity index 92%
rename from agent_space/patches/agent-space-layout.md
rename to agent_space/patches/20260831-001.md
--- a/agent_space/patches/agent-space-layout.md
+++ b/agent_space/patches/20260831-001.md
@@
-# agent-space-layout
+# 20260831-001
@@
-이 작업에서 다음 파일도 함께 추가되었으나, 자기 자신을 포함하는 재귀적인 diff를 피하기 위해 `agent-space-layout.patch`에는 포함하지 않았다.
+이 작업에서 다음 파일도 함께 추가되었으나, 자기 자신을 포함하는 재귀적인 diff를 피하기 위해 `20260831-001.patch`에는 포함하지 않았다.
@@
-- `agent_space/patches/agent-space-layout.patch`
-- `agent_space/patches/agent-space-layout.md`
+- `agent_space/patches/20260831-001.patch`
+- `agent_space/patches/20260831-001.md`
@@
-`patches.md`는 `patches` 경로의 용도를 설명하며, `agent-space-layout.patch`와 이 문서는 이번 변경을 기록하는 동명 쌍이다.
+`patches.md`는 `patches` 경로의 용도를 설명하며, `20260831-001.patch`와 이 문서는 이번 변경을 기록하는 동명 쌍이다.

diff --git a/agent_space/AGENT.md b/agent_space/AGENT.md
--- a/agent_space/AGENT.md
+++ b/agent_space/AGENT.md
@@
 - diff 패치 파일과 설명 Markdown 파일은 같은 이름으로 쌍을 이루도록 할 것. 확장자만 각각 패치 형식과 `.md`로 구분할 것.
+- 패치 쌍의 기본 이름은 `YYYYMMDD-NNN` 형식을 사용할 것.
+  - `YYYYMMDD`는 패치를 기록한 날짜다.
+  - `NNN`은 해당 날짜 안에서의 3자리 순번이며 `001`부터 증가한다.
+  - 파일 이름에는 변경 내용, 기능명, 모듈명, 구현 방식 같은 의미 정보를 넣지 말 것.
+  - 순번은 기록의 시간적 순서를 나타내는 메타정보로만 사용할 것.
 - 실제 diff 패치에 포함되지 않은 관련 변경사항, 전제, 후속 작업 또는 누락 사항이 있더라도 설명 Markdown에는 반드시 기록할 것.

diff --git a/agent_space/patches/patches.md b/agent_space/patches/patches.md
--- a/agent_space/patches/patches.md
+++ b/agent_space/patches/patches.md
@@
 모든 수정은 `.patch` 파일과 동일한 기본 이름의 `.md` 설명 파일을 한 쌍으로 기록합니다. 설명 파일에는 변경 목적, 적용 방향, 해당 방향을 선택한 이유, 다른 방향을 선택하지 않은 이유, 패치에 포함되지 않았지만 함께 발생한 변경사항을 기록합니다.
+
+## 이름 규칙
+
+패치 쌍의 기본 이름은 `YYYYMMDD-NNN` 형식을 사용합니다.
+
+- `YYYYMMDD`: 기록 날짜
+- `NNN`: 같은 날짜 안에서 `001`부터 증가하는 3자리 순번
+- 파일 이름에는 변경 내용, 기능명, 모듈명, 구현 방식 등 의미 정보를 넣지 않습니다.
+- 패치가 무엇을 변경했는지는 동명 `.md` 설명 파일에서 확인합니다.
+
+예:
+
+```text
+20260831-001.patch
+20260831-001.md
+20260831-002.patch
+20260831-002.md
+```
