# 20260911-019 — audit Mantine ownership cutover correction

## 목적
20260911-018의 audit cutover를 올바른 unified-diff line count로 적용한다.

## 변경
- required DOM audit를 `dashboard`, `dashboardCaption`, `graphArea`, `readmeContent`로 한정.
- legacy overlay panel/toggle/popup/readme-dialog audit 제거.

## 이전 실패
018은 첫 hunk header의 line count가 실제 8→10줄과 달리 7→9줄로 표기되어 patch parser가 다음 hunk를 읽지 못했다. 애플리케이션 소스에는 적용되지 않았다.

## 영향/제외
FSM/model/file-format/renderer invariant는 유지한다. 기존 `REQUIRED_CONTROL_IDS` 정의는 후속 dead-code 제거 대상으로 남긴다.

## 검증
workflow patch check, JS syntax, portable build, split/portable smoke.