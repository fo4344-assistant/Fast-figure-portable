# 20260911-005 — Disconnect legacy graph UI execution paths, context fix

## Purpose

Apply the graph UI execution cutover from `003`/`004` after correcting the exact source context before the legacy `#addGraphObject` handler. Both earlier patches failed at `git apply --check` and changed no application source.

## Correction

The previous hunk copied the graph-object delete handler terminator as `});`; the source actually ends that callback assignment with `};` before the surrounding `.forEach()` closes. This patch uses the exact source context and otherwise preserves the same intended deletion set.

## Changes and boundaries

Remove legacy graph object/input/palette/axis/file-action event wiring, graph-only lifecycle wrappers, and graph DOM startup initialization. Preserve model/EFSM commands, Plotly rendering, FFSX/Plotly parser/serializer/import/export functions, and Mantine graph UI paths.

## File-format compatibility

No FFPX, FFSX, Plotly JSON, image, or CSV file-format behavior changes.

## Validation

Repository CI must pass patch application, JavaScript syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert this patch's application commit. 003/004 need no application rollback because they never applied.

## Follow-up

Delete the newly dead legacy graph helper/form-reader/renderer code and then remove the `#buildBox` markup, required IDs, and graph-only CSS.
