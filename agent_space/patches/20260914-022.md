# 20260914-022 — Split legacy layout helper cleanup into non-overlapping hunks

## Purpose
Apply the same dead-layout cleanup with a patch structure that avoids the malformed large hunk used by 019–021. None of those attempts changed source.

## Exact changes
- Remove the single `renderLayout()` call at the end of dashboard rendering in its own hunk.
- Delete the exact 129-line contiguous block containing `syncPopupBounds`, `syncLayoutMapSize`, `installLayoutResizer`, and `renderLayout` while retaining the dashboard closing brace.
- Remove the remaining project-render and Mantine call sites.
- Remove optional `layoutMap` style/geometry writes.

## Rationale
The code change is sound; prior failures were unified-diff formatting/count issues. This patch uses a non-overlapping 4→3 call-site hunk and a 131→2 helper-block hunk (129 deleted lines plus two context lines).

## Rejected alternatives
- Further approximate the prior combined hunk: rejected.
- Keep dead code because patch formatting was inconvenient: rejected.

## Source of truth
Project layout/EFSM state remains unchanged.

## EFSM/core impact
No domain behavior change intended.

## Validation
Patch check/apply, JS syntax, portable build, split/portable Chromium smoke tests.

## Rollback
Recovery source commit remains `0bd57c81bb6e5ef9d8ef6125936d2fd4d124d1ca`; patches 019–021 failed before source application.

## Follow-up
Proceed to annotation helpers, export fallbacks, graph compatibility DOM, and CSS cleanup.