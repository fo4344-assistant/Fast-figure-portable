# 20260910-034 — Correct legacy image-selection panel removal

## Purpose

Reissue the intended `20260910-033` cleanup with corrected unified-diff metadata. `033` failed at `git apply` because the `syncSlotContentTypeUi()` hunk declared four new lines while containing three. No application source was changed by `033`.

The intended change removes the legacy sidebar image-selection/blank-image UI while retaining the shared blank-image command used by Mantine. Legacy UI compatibility is not retained; file-format compatibility is unchanged.

## Exact changes
- Remove `#imageSelectionPanel` and `#insertEmptyImage` from `Fast-figure.html`.
- Remove both IDs from `REQUIRED_CONTROL_IDS`.
- Remove the legacy disabled-state write in `refreshImageControls()`.
- Remove image-selection-panel visibility synchronization from `syncSlotContentTypeUi()`.
- Remove the legacy `#insertEmptyImage.onclick` binding.
- Retain `insertEmptyImageIntoSelectedSlot()` unchanged for direct Mantine use.

## Correction from 033

Only unified-diff hunk metadata is corrected. The application change intent and source edits are otherwise identical to `033`.

## Source of truth

Selected slot/content type remain in FSM/project state. Mantine owns the blank-image action and directly invokes `insertEmptyImageIntoSelectedSlot()`.

## EFSM / core impact

None. No transition, project model, image renderer, parser, serializer, or command behavior changes.

## File-format compatibility

No FFPX, FFSX, Plotly JSON, image-byte packaging, import/export, or versioning code is modified.

## Rejected alternatives
- Repair `033` in place: rejected to preserve failed-patch audit history.
- Keep a hidden legacy button: rejected because legacy UI compatibility is not required.
- Recreate a Mantine-to-DOM click bridge: rejected because direct command invocation already exists.

## Validation

Repository CI must pass patch application, JavaScript syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert the `034` application commit to restore the retired panel/button and binding. No data migration is involved.

## Follow-up

Remove the legacy image settings fieldset and form-reader/sync handlers while retaining `normalizeImageSettings()`, `syncSlotImageElement()`, and `applyImageSettingsFromValues()` as functional/model-rendering boundaries.
