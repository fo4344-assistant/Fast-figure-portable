# 20260911-022 — legacy overlay audit removal correction

## 목적
20260911-021의 legacy overlay audit 제거를 정확한 unified-diff line count로 적용한다.

## 변경
- legacy overlay panel visibility 검사 제거.
- legacy toggle highlight/aria-expanded 검사 제거.
- `dashboard-popup-open`, `layout-open` class 검사 제거.
- legacy README dialog open 검사 제거.

## 이전 실패
021은 해당 hunk가 실제 50→3줄인데 51→3줄로 표기되어 `git apply` parser가 corrupt patch로 중단했다. 애플리케이션 소스에는 반영되지 않았다.

## 유지
FSM state 유효성, project/model/asset/chart/slot invariant, renderer 및 FFPX/FFSX/Plotly 계층은 유지한다.

## 검증
workflow patch check, JS syntax, portable build, split/portable smoke.

## 롤백
이 패치를 되돌리면 legacy overlay audit 검사가 복구된다.