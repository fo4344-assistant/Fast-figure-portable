# 20260916-003 — Retry print/export DOM-fallback removal with generated exact hunks

## Purpose
Recover from the pre-application failure of `20260916-002` and apply the same print/export ownership cleanup using exact current-source hunks generated and validated against the verified `main` line context.

## Failed-attempt recovery
- `20260916-002.patch` and its explanation remain as audit history.
- Its workflow failed at `git apply --check` before modifying application source.
- The failure was patch syntax/construction: Git reported `patch fragment without header` at the final hunk.
- No source rollback is required.
- This patch preserves the same intended code change but regenerates the unified diff from exact current `fast-figure.js` excerpts and validates it with a synthetic `git apply --check` before queueing.

## Exact changes
- In `exportDashboard(capture = false, options = null)`:
  - replace `printStatus` fallback with an optional `options.onStatus` sink;
  - replace `printWidth` / `printHeight` reads with option values and current rendered geometry fallback;
  - replace `printDpi` / `printFormat` reads with option/default values;
  - remove native control writeback.
- In `exportDashboardTarget(options = null)`:
  - replace `printStatus` fallback with an optional `options.onStatus` sink;
  - replace native width/height/DPI/format reads with option/default values;
  - remove native width/DPI/height writeback.

No canvas composition, Plotly rendering, image rendering, caption/label geometry, DPI metadata encoding, raster-limit validation, filename construction, or lifecycle event behavior is changed.

## Why this direction
The native print controls no longer exist and `FastFigurePrintOverlay` already supplies explicit options. The export functions are functional renderer/command boundaries and should not depend on removed UI DOM. This removes only dead editor ownership while retaining robust defaults for direct invocation.

## Rejected alternatives
- Retry `20260916-002` unchanged: rejected because its unified diff was malformed and already failed pre-application.
- Edit `fast-figure.js` directly through the contents API: rejected because project policy requires application source changes to be applied through paired diff patches.
- Rewrite or relocate export algorithms into Mantine: rejected because export rendering is UI-independent and remains live.
- Reintroduce hidden native controls: rejected because that would recreate a dead compatibility bridge and duplicate UI ownership.

## Source of truth
Project/model snapshots and renderer geometry remain authoritative for export content. `FastFigurePrintOverlay` owns print input drafts. Export functions consume passed values and optional status callbacks without owning UI state.

## EFSM/core impact
No intended transition, schema, persistence, or file-format change. Existing `PRINT_EXPORT` and `CAPTURE_EXPORT` lifecycle events, FFPX/FFSX semantics, export validation, target geometry, caption/label rendering, and DPI limits are preserved.

## Validation
Before queueing, the regenerated patch was applied with `git apply --check`, `git apply`, and `git diff --check` against a synthetic file containing the exact current source excerpts at the verified repository line positions.

The repository workflow must still pass:

1. `git apply --check` and `git diff --check`
2. JavaScript syntax checks
3. portable HTML build
4. split and portable Chromium `file://` smoke tests
5. source inspection confirming executable references to `printStatus`, `printWidth`, `printHeight`, `printDpi`, and `printFormat` are removed
6. confirmation that Mantine save/capture callers remain unchanged

## Rollback
Revert commit `Apply agent patch 20260916-003`.

No data migration or state rollback is required.

## Follow-up
After successful application, remove orphan native print CSS (`print-panel`, `print-controls`, `print-actions`, obsolete print-toggle styling) in a separately audited patch while preserving layout CSS and actual export renderer logic. Then proceed to graph compatibility DOM cutover.
