# 20260903-008 — workspace domain sync 분리 재시도

## 상태

`20260903-007`은 `git apply --check`에서 `corrupt patch at ...:31`로 실패했고 HTML에는 적용되지 않았다.

원인은 작은 hunk로 분리하면서 hunk header의 old/new line count를 잘못 기록한 것이다. 기계적으로 실제 줄 수를 다시 계산했다.

- project hunk: `7/13`
- graph hunk: `8/19`
- image hunk: `11/18`
- graph-exit hunk: `11/14`

## 목적

`007`과 동일하게 workspace entry의 domain/transient synchronization과 legacy native presentation을 분리한다.

## 변경 내용

- 앱 버전 `1.1.107-alpha` → `1.1.108-alpha`
- `syncProjectWorkspaceDomainState()`
- `syncGraphWorkspaceDomainState()`
- `syncImageWorkspaceDomainState()`
- `exitGraphWorkspaceDomainState()`
- 기존 `sync*WorkspaceState()`는 위 domain helper 결과를 받아 legacy presentation 수행

## 007과의 차이

기능 설계 변경 없음. 각 unified diff hunk header count만 실제 patch 내용에 맞게 교정했다.

## 검증

- hunk actual old/new line count 기계 검산 완료
- `git apply --check`
- `git diff --check`
- application JavaScript `node --check`
- workspace/README-close/active CSV/image/graph selection 의미 유지

## rollback

`007` 설명과 동일하다.