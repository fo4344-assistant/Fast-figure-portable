# 20260903-011 — lifecycle / overlay 정책 helper 재시도

## 상태

`20260903-010`은 `git apply --check`에서 `corrupt patch at ...:25`로 실패했고 HTML에는 적용되지 않았다.

원인은 helper 9줄을 추가하면서 context 1줄을 포함한 new hunk count를 `9`로 기록한 것이다. 실제 hunk count는 old/new `1/10`이다.

## 목적과 변경 내용

`010`과 동일하다.

- 앱 버전 `1.1.108-alpha` → `1.1.109-alpha`
- `applyLifecycleDomainState({ state, payload })`
- `applyOverlayOpenPolicy(state)`

## 010과의 차이

기능 변경 없음. unified diff hunk header를 `1/10`으로 교정한다.

## 검증

- hunk count 재검산
- `git apply --check`
- `git diff --check`
- application JavaScript `node --check`

## rollback

`010` 설명과 동일하다.