# 20260914-024 — Apply layout helper cleanup with exact current locations

## Purpose
Apply the already-reviewed native-layout cleanup using exact current source locations after 019–023 failed before source modification.

## Exact changes
- Remove the dashboard `renderLayout()` call at its actual current line.
- Delete the contiguous four-function native layout DOM helper block.
- Remove the project-render `renderLayout()` call.
- Remove Mantine calls to the deleted helper/projection functions using the current source locations.
- Stop `applySlotStyle` from referencing removed `layoutMap` DOM.

## Rationale
The previous patch is now syntactically valid, but its hunk start positions for a low-context deletion did not match the current file. Current source was re-read directly and this patch uses those positions and stable surrounding context.

## Rejected alternatives
- Add more fuzz/context guessing: rejected in favor of current-source exact locations.
- Retain the obsolete DOM helper layer: rejected.

## Source of truth
Project layout state and existing EFSM/project commands.

## EFSM/core impact
No intended domain behavior change.

## Validation
Patch check/apply, JS syntax, portable build, split/portable Chromium `file://` smoke tests.

## Rollback
Recovery source commit remains `0bd57c81bb6e5ef9d8ef6125936d2fd4d124d1ca`; 019–023 never changed source.

## Follow-up
Annotation DOM helpers, print fallback cleanup, graph compatibility DOM cutover, markup and CSS cleanup.