# 20260911-017 — dead legacy overlay helper removal

## 목적
016에서 overlay entry/exit가 DOM 비의존으로 전환되어 `setOverlayDomState()`가 더 이상 실행되지 않는다. dead helper를 제거해 old panel/toggle/dialog ownership을 코드에서도 축소한다.

## 변경
- `setOverlayDomState()` 전체 제거.

## 유지
- `appFSM.state.overlay`와 overlay state/event 정의.
- Mantine overlay components.
- README content provider.
- 파일 포맷/renderer/model.

## 검증
workflow patch check, JS syntax, portable build, split/portable smoke.

## 롤백
이 패치만 되돌리면 helper 정의만 복구된다. 016을 함께 되돌리지 않는 한 실행 경로는 복구되지 않는다.