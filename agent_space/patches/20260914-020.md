# 20260914-020 — Correct dead legacy layout helper removal

## Purpose
Apply the same layout-DOM cleanup described in 019 with corrected unified-diff hunk counts. Patch 019 failed before application because its large deletion hunk was malformed; no source change occurred.

## Exact changes
- Delete `syncPopupBounds`, `syncLayoutMapSize`, `installLayoutResizer`, and `renderLayout`.
- Remove remaining calls from dashboard/project rendering and Mantine layout/sidebar code.
- Stop `applySlotStyle` from addressing removed `layoutMap` DOM.
- Preserve layout state, geometry, merge/split, zoom, dashboard rendering, and export logic.

## Rationale
The functions only targeted the already deleted native layout panel. The Mantine overlay renders its preview directly from project state.

## Rejected alternatives
- Retry the malformed 019 patch unchanged: rejected because the workflow identified a corrupt hunk header.
- Keep no-op legacy helpers: rejected because they falsely retain deleted UI dependencies.

## Source of truth
Existing project layout state and EFSM/project commands remain authoritative.

## EFSM/core impact
No domain behavior change intended.

## Validation
Repository patch check/apply, JS syntax, portable build, and split/portable Chromium smoke tests.

## Rollback
The source is still at recovery commit `0bd57c81bb6e5ef9d8ef6125936d2fd4d124d1ca` plus the non-applied 019 queue commits. Revert the eventual 020 apply commit or restore that recovery commit.

## Follow-up
Remove dead label/caption DOM helpers, then clean export fallbacks and compatibility graph DOM.