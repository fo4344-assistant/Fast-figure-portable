# 20260911-010 — Complete remaining Mantine interaction surfaces (corrected)

## Purpose

Apply the same Mantine-completion work intended by `20260911-009`, correcting only the unified-diff representation. `009` failed at `git apply --check` before any application source was modified because two adjacent layout-preview hunks overlapped after the first hunk changed their shared context.

## Exact changes

### `fast-figure.js`
- Add a UI-independent telemetry snapshot/subscription channel for status text and debug output.
- Route `status()` through that telemetry channel while retaining temporary WIP DOM projection.
- Publish debug lines through the same channel.
- Add UI-independent `setDebugEnabled()`, `clearDebugOutput()`, and `saveDebugOutput()` commands.

### `fast-figure-ui.js`
- Add `useUiTelemetry()` and Mantine `FastFigureStatusDebug`.
- Show status with `aria-live` and provide debug enable/disable, save, clear, and visible log output.
- Add direct pointer dragging to the Mantine label preview with selected-slot geometry conversion and clamp.
- Render layout preview using `dashboardGeometry()` for aspect, gap, outer margin, and radius.
- Add a Mantine-owned layout preview resize handle whose local size does not alter project state.
- Mount/export `FastFigureStatusDebug` in the Mantine shell.

## Difference from `20260911-009`

No intended application behavior differs. The two adjacent layout preview diff fragments are represented as one unified hunk so the repository workflow can apply them deterministically.

## Source of truth

- Status/debug transient state: UI telemetry channel in `fast-figure.js`.
- Label position: `activeProject.labelSettings`.
- Layout geometry: project layout model plus `dashboardGeometry()` / `gridSlotGeometry()`.
- Persistent application state: existing project model and `appFSM`.

## EFSM / core impact

No EFSM transition, persistent project model, chart renderer, image renderer, or file schema semantics change. Telemetry is transient and non-persistent.

## File-format compatibility

No FFPX, FFSX, Plotly JSON, CSV, image, parser, serializer, manifest, or package-version behavior changes.

## Validation

Repository CI must pass patch application, JavaScript syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert this patch's application commit. No stored-data migration is involved.
