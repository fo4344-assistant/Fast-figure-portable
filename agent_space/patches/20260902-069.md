# 20260902-069 — graph slot file/action read + bridge 경계 재시도

## 상태

`20260902-068`은 `git apply --check`에서 `corrupt patch at ...:54`로 실패했고 HTML에는 적용되지 않았다.

원인은 마지막 hunk의 line count 오류다. 실제 old/new line 수는 `14/26`인데 `15/27`로 기록되어 있었다.

## 목적

068과 동일하게 graph slot file/action Mantine component를 위한 순수 read model과 bridge를 준비한다.

## 변경 내용

- 앱 버전 `1.1.100-alpha` → `1.1.101-alpha`
- `selectedGraphFileActionsModel(slot)` 추가
- bridge `readGraphFileActions()` 추가
- bridge `setGraphEditorEditable(editable, chartId)` 추가
- editable command는 `067`의 `setGraphEditorEditableFromValue()` 재사용

## 068과의 차이

기능 설계 변경 없음. unified diff hunk count만 실제 내용에 맞게 교정한다.

## 검증

- `git apply --check`
- `git diff --check`
- application JavaScript `node --check`
- read model DOM/domain mutation 없음
- file schema/renderer diff 없음

## rollback

068 설명과 동일하다.
