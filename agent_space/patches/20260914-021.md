# 20260914-021 — Apply corrected legacy layout helper cleanup

## Purpose
Apply the layout helper cleanup from 019/020 with the large hunk's exact unified-diff line counts. Both prior attempts failed before modifying source.

## Exact changes
- Delete native-layout-only DOM helpers and calls.
- Remove `layoutMap` writes from slot-style projection.
- Remove obsolete layout preview refresh calls from Mantine code.
- Preserve all layout model, geometry, dashboard, merge/split, zoom, and export behavior.

## Rationale
The previous two attempts were patch-format failures only. Recounting the large hunk gives 136 old lines and 6 retained context lines.

## Rejected alternatives
- Keep retrying approximate hunk sizes: rejected; this patch uses the exact count.
- Keep dead helpers: rejected because the corresponding DOM has already been removed and smoke-tested.

## Source of truth
Existing project layout state and EFSM/project commands.

## EFSM/core impact
No domain behavior change intended.

## Validation
Patch check/apply, JS syntax, portable build, split/portable Chromium smoke tests.

## Rollback
Source recovery point remains `0bd57c81bb6e5ef9d8ef6125936d2fd4d124d1ca` (`Apply agent patch 20260914-018`); 019 and 020 did not apply source changes.

## Follow-up
Continue with dead annotation helpers, export fallbacks, graph compatibility DOM, then dead CSS.