# 20260910-029 — Supersede malformed legacy reset/delete patch

## Purpose

Supersede malformed patch `20260910-028` with the same intended removal of legacy reset/delete asset controls using corrected unified-diff hunk counts.

Legacy UI compatibility is not retained. FFPX/FFSX compatibility remains untouched.

## 20260910-028 failure

`20260910-028` failed before application with `corrupt patch`. No application source was changed. The first HTML hunk and the reset-handler hunk declared incorrect old/new line counts. This replacement corrects only patch structure; the functional intent is unchanged.

## Exact changes

### `Fast-figure.html`
- Remove `#resetSelectedSlot`.
- Remove `#deleteSelectedAsset`.

### `fast-figure.js`
- Remove both controls from `REQUIRED_CONTROL_IDS`.
- Remove `updateFileAvailability()` synchronization of `#resetSelectedSlot` visibility.
- Remove the legacy `#resetSelectedSlot.onclick` native-confirm handler.

## Source of truth

`FastFigureDataActions` owns reset/delete availability, Mantine confirmation UI, and dispatch to the existing project/FSM operations.

## File-format compatibility

No FFPX/FFSX reader/writer, package layout, versioning, serialization, or import logic changes.

## EFSM / core impact

None. Existing `SLOTS_RESET`, asset deletion, graph-object replacement, and selection events remain authoritative.

## Rationale

The legacy buttons and native confirmation path duplicate the Mantine UI. Because legacy UI compatibility is explicitly out of scope, the duplicate entrypoint should be removed rather than hidden or maintained.

## Rejected alternatives

- Hidden fallback buttons: rejected.
- Native-confirm helper retained without markup: rejected.
- Direct React mutation of project objects: rejected; existing FSM/project model remains authoritative.

## Validation

Repository CI must pass patch application, JavaScript syntax checks, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert the application commit for `20260910-029` to restore the legacy buttons and native reset path. Project/file-format data is unaffected.
