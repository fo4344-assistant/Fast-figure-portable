# 20260911-025 — FSM legacy UI hook removal correction

## 목적
20260911-024의 Mantine single-owner FSM cutover를 정확한 unified-diff line count로 적용한다.

## 변경
- empty `syncAssetSelectionState`, no-op `syncProjectWorkspaceState`, no-op overlay entry/exit helpers 제거.
- project workspace의 no-op entry/update 제거.
- overlay states의 legacy entry/exit/update DOM hooks 제거.
- assetSelection states의 empty entry/update hooks 제거.

## 이전 실패
024는 첫 hunk가 실제 4→2줄인데 5→3줄로 표기되어 patch parser 단계에서 실패했다. 애플리케이션 소스에는 반영되지 않았다.

## 유지
graph/image transient model selection, FSM transition/event, renderer, project model, FFPX/FFSX/Plotly 계층은 유지한다.

## 검증
workflow patch check, JS syntax, portable build, split/portable smoke.

## 롤백
이 패치를 되돌리면 legacy FSM UI hooks가 복구된다.