# 20260910-032 — Correct legacy slot-type control removal

## Purpose

Reissue the intended `20260910-031` cleanup with valid unified-diff hunk counts. `031` failed during `git apply --check` with `patch fragment without header`; it did not modify application source.

The functional intent is unchanged: remove the retired legacy sidebar slot-type selector and file-type hint because Mantine already owns this generic UI. Legacy UI compatibility is not retained. File-format compatibility remains untouched.

## Exact changes

### `Fast-figure.html`
- Remove `.slot-type-controls` CSS.
- Remove `#slotTypeControls`, `#slotTypeGraph`, and `#slotTypeImage` markup.
- Remove `#fileTypeHint` markup.

### `fast-figure.js`
- Remove those controls from `REQUIRED_CONTROL_IDS`.
- Stop synchronizing their visual/text state in `syncSlotContentTypeUi()`.
- Keep the remaining image/CSV legacy synchronization temporarily until those active legacy editor surfaces are removed in their own cutover.
- Remove the legacy graph/image slot-type onclick handlers.
- Preserve `setSlotContentType()` as the UI-independent command used by Mantine.

## Correction from 031

`031` encoded incorrect old/new line counts for its HTML hunk, causing the parser to treat the following hunk header as body text. `032` corrects every hunk count while retaining the same source edits. No application source from `031` was applied.

## Source of truth

Slot content type remains project/FSM state. Mantine `FastFigureDataActions` is the sole generic UI owner for choosing graph versus image, and calls `setSlotContentType()` directly.

## EFSM / core impact

None. No state transition, project schema, renderer algorithm, parser, serializer, or model semantics are changed.

## File-format compatibility

No FFPX, FFSX, Plotly JSON, CSV, image import/export, package versioning, or stored project representation is changed.

## Rejected alternatives

- Repair `031` in place: rejected because failed patch records are retained for auditability; a new numbered patch makes the failure and correction explicit.
- Restore hidden legacy controls: rejected because legacy UI compatibility is outside the compatibility boundary.
- Route Mantine through the old controls: rejected because that would recreate the retired DOM bridge.

## Validation

Repository CI must pass patch application, JavaScript syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert the application commit for `032`. No file data migration is involved.

## Follow-up

Continue removing legacy CSV/image selection and image editor DOM synchronization, then the graph editor facade, while preserving the independent file-format read/write functions.
