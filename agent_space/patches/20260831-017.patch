diff --git a/Fast-figure.html b/Fast-figure.html
--- a/Fast-figure.html
+++ b/Fast-figure.html
@@ -4,4 +4,51 @@
         if (!runtime) throw new Error("Fast Figure UI runtime failed to load");
-        const root = runtime.createRoot(document.getElementById("ff-react-bootstrap-root"));
-        root.render(runtime.React.createElement(runtime.MantineCore.MantineProvider, { defaultColorScheme: "light" }, runtime.React.createElement(runtime.React.Fragment, null)));
+        const { React, createRoot, MantineCore } = runtime;
+        const { ActionIcon, Button, Input, MantineProvider, createTheme } = MantineCore;
+        const fastFigureTheme = createTheme({
+          fontFamily: 'system-ui, "Segoe UI", sans-serif',
+          fontSizes: { ff: "14px" },
+          radius: { ff: "6px" },
+          defaultRadius: "ff",
+          components: {
+            Button: Button.extend({
+              defaultProps: { size: "ff" },
+              vars: (_theme, props) =>
+                props.size === "ff"
+                  ? {
+                      root: {
+                        "--button-height": "38px",
+                        "--button-padding-x": "12px",
+                        "--button-fz": "14px",
+                      },
+                    }
+                  : { root: {} },
+            }),
+            ActionIcon: ActionIcon.extend({
+              defaultProps: { size: "ff" },
+              vars: (_theme, props) =>
+                props.size === "ff" ? { root: { "--ai-size": "38px" } } : { root: {} },
+            }),
+            Input: Input.extend({
+              defaultProps: { size: "ff" },
+              vars: (_theme, props) =>
+                props.size === "ff"
+                  ? {
+                      wrapper: {
+                        "--input-height": "38px",
+                        "--input-fz": "14px",
+                        "--input-radius": "6px",
+                      },
+                    }
+                  : { wrapper: {} },
+            }),
+          },
+        });
+        const root = createRoot(document.getElementById("ff-react-bootstrap-root"));
+        root.render(
+          React.createElement(
+            MantineProvider,
+            { defaultColorScheme: "light", theme: fastFigureTheme },
+            React.createElement(React.Fragment, null),
+          ),
+        );
         window.fastFigureUiRoot = root;
@@ -20,3 +67,3 @@
           <p><strong>Application version</strong></p>
-          <p><code>1.1.30-alpha</code></p>
+          <p><code>1.1.31-alpha</code></p>
           <p><strong>Developer</strong></p>
@@ -34,3 +81,3 @@
       const PACKAGE_FORMAT_VERSION = 3;
-      const APP_BUILD = "1.1.30-alpha";
+      const APP_BUILD = "1.1.31-alpha";
       const ICONOIR_GLYPHS = Object.freeze({
