# 20260911-008 — Remove legacy graph header/global/axis wiring, corrected diff

## Purpose

Apply the graph execution cutover intended by `20260911-007` after correcting malformed unified-diff hunk line counts. `007` failed before patch application and changed no application source.

## Exact changes

### `fast-figure.js`
- Remove startup `fillGraphFontSelect("graphFontFamily")`.
- Remove legacy `#headerLines` onchange wiring.
- Remove old showLegend/showTitle/showZeroLine button handlers.
- Remove delegated `.axis-fields` change, Enter-key, and setting-toggle handlers.
- Remove graph-only lifecycle-wrapper entries for slot import, palette import, FFSX export and Plotly export.
- Stop startup synchronization/installation of graph-only toggle state, `axisFields()`, `graphColorInputs()`, and `syncEditableUi(null)`.
- Preserve graph model commands, Plotly rendering, FFSX/Plotly import/export functions, data parsing, normalization, and Mantine controls.

## Rationale

The remaining listeners maintain an obsolete DOM presentation authority. Mantine already owns these controls through project-model state and UI-independent graph commands. Legacy UI compatibility is not a project requirement.

## Source of truth

- Persistent graph state: project chart model / `appFSM`.
- Graph presentation and editing: Mantine.
- Rendering and file compatibility: existing non-React functions.

## EFSM / core impact

None. No FSM event semantics, graph schema, renderer, parser, serializer, or persistent state rules change.

## File-format compatibility

FFPX, FFSX, Plotly JSON, CSV, and image formats are unchanged.

## Validation

Repository CI must pass patch application, JavaScript syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert this patch's application commit. `007` requires no application rollback because it never applied.

## Follow-up

Decouple remaining data-selection/runtime helpers from graph DOM defaults, remove dead graph form readers/renderers/helpers, delete `#buildBox` and graph-only CSS/markup, then audit remaining non-graph Mantine migration work.
