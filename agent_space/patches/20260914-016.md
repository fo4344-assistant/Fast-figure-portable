# 20260914-016 — Remove legacy label panel markup

## Purpose
Remove the native legacy label-settings panel after Mantine ownership and legacy event detachment.

## Exact changes
- Delete `section#labelPanel` and its preview/settings controls from `Fast-figure.html`.
- Preserve project label state, slot-label rendering, export label rendering, and Mantine label controls.

## Rationale
The native panel no longer owns label interactions. `renderDashboard()` already guards the optional legacy preview element, so removing this panel does not remove label rendering from the dashboard.

## Rejected alternatives
- Keep a hidden compatibility panel: rejected because the panel is no longer a state source or active controller.
- Remove label-domain helpers simultaneously: rejected; domain/render helpers are classified separately after markup removal.

## Source of truth
`activeProject.annotations.labels` / existing label project state remains authoritative through the EFSM/Mantine path.

## EFSM/core impact
None intended. Markup deletion only.

## Validation
Repository apply check, JS syntax, portable build, split and portable Chromium `file://` smoke tests.

## Rollback
Pre-deletion recovery commit: `9a9be3d2e25a44d8749149100ddb1e4522c95b9f` (`Apply agent patch 20260914-015`).

## Follow-up
After success, verify the new apply commit as recovery point and remove the legacy caption panel.