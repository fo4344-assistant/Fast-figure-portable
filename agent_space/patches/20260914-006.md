# 20260914-006 — Mantine layout and palette command ownership (count corrected)

## 목적
20260914-005가 마지막 palette-handler hunk의 줄 수 표기 오류로 적용되지 않았으므로 동일 변경을 정확한 unified-diff count로 적용한다.

## 변경
- zoom lock의 legacy control DOM write 제거.
- old zoom/layout/merge/split handler 제거.
- `uiPaletteFromControls()` 제거.
- `applyUiPalette()`를 project palette 단일 source로 전환.
- old palette apply/reset handler 제거.

## Source of truth
layout은 `activeProject.layout`/`slotStyle`, palette는 `activeProject.appearance.uiPalette`, zoom은 기존 runtime state다.

## EFSM/core 영향
FFPX/FFSX/Plotly, project schema와 layout 알고리즘은 변경하지 않는다.

## 검증
workflow 전체 검증을 통과해야 한다.

## 롤백
patch revert로 복구 가능하며 persistent migration은 없다.

## WIP
sidebar/debug/lifecycle startup wiring과 legacy HTML/CSS는 후속 대상이다.
