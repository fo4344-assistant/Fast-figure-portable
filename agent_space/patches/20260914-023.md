# 20260914-023 — Final corrected legacy layout helper cleanup

## Purpose
Apply the dead native-layout cleanup with all unified-diff hunk sizes matched to their actual bodies. Patches 019–022 failed before source application.

## Exact changes
- Remove `renderLayout()` calls tied to deleted native preview DOM.
- Delete the 129-line block containing `syncPopupBounds`, `syncLayoutMapSize`, `installLayoutResizer`, and `renderLayout`.
- Remove optional `layoutMap` writes from `applySlotStyle`.
- Remove stale Mantine calls to `renderLayout` and `syncLayoutMapSize`.

## Rationale
The prior 022 core hunk was correct, but four small `fast-figure-ui.js` hunks declared more context lines than they contained. This patch corrects those headers to 5→4, 5→4, 6→5, and 6→5 respectively.

## Rejected alternatives
- Preserve dead native-layout code: rejected because the corresponding DOM is already removed.
- Change layout algorithms while cleaning UI code: rejected; domain/layout behavior remains untouched.

## Source of truth
Project layout state, `layoutSelected`, and existing EFSM/project commands remain authoritative.

## EFSM/core impact
No intended domain behavior change.

## Validation
Patch check/apply, JS syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback
Recovery source commit remains `0bd57c81bb6e5ef9d8ef6125936d2fd4d124d1ca`; patches 019–022 did not modify source.

## Follow-up
Continue annotation helper cleanup, print fallback cleanup, graph compatibility DOM cutover, markup deletion, and CSS deletion.