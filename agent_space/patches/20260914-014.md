# 20260914-014 Legacy shell markup deletion retry

## 목적
20260914-013과 동일한 legacy shell markup 삭제를 hunk overlap 없이 다시 적용한다. 013은 `git apply --check` 단계에서 실패했으므로 앱 소스에는 어떤 삭제도 적용되지 않았다.

## 변경
- legacy 상단 header/toolbar 삭제
- legacy project fieldset 삭제
- legacy UI palette/debug/README sidebar controls 삭제
- native README dialog wrapper와 close button 삭제, canonical `template#readmeContent`는 보존
- legacy sidebar resize/collapse handle 삭제
- graph compatibility controls와 layout/label/caption/print panel은 이번 패치에서 보존

## 이유
013의 기능 방향에는 문제가 없었고 실패 원인은 `</aside>`가 연속 hunk의 context로 중복된 patch 구조였다. 이번 패치는 palette/debug 삭제와 README dialog-open 삭제를 하나의 hunk로 합쳐 overlap을 제거한다.

## Source of truth
generic shell/project/palette/debug/README UI는 Mantine `fast-figure-ui.js`, README 본문은 `template#readmeContent`, domain/FSM/renderer는 기존 project model과 `fast-figure.js`이다.

## EFSM/core 영향
없음. markup 삭제만 수행한다.

## 삭제 전 복구 지점
삭제 직전 확인된 복구 커밋은 `e6165770847ffcce78845b6f89ef7afe1242962c` (`Apply agent patch 20260914-012`)이다. GitHub에서 직접 조회했으며 부모 `0e2ab6b6138ff95c9674b548b35a53d68140f0f3`도 존재한다. 013 실패로 이 복구 지점 이후 앱 소스 변경은 없다.

## 검증
`git apply --check`, JavaScript syntax, portable build, split/portable Chromium smoke를 모두 통과해야 한다.

## 롤백
`e6165770847ffcce78845b6f89ef7afe1242962c`로 되돌리면 삭제 전 상태를 복구할 수 있다.

## WIP / 후속
다음 단계에서 layout/label/caption/print legacy panel markup과 dead CSS/helper를 별도 삭제한다. graph compatibility controls는 core DOM read가 제거되기 전까지 보존한다.
