# 20260911-004 — Disconnect legacy graph UI execution paths, corrected patch

## Purpose

Apply the legacy graph UI execution cutover described in `20260911-003` with stronger exact context for the graph-object handler hunks. `003` failed during `git apply --check` and changed no application source.

## Exact changes

- Remove the legacy graph-object outside-click and add-object DOM action wiring.
- Remove legacy graph input bindings, graph palette controls, FFSX/Plotly button/input bindings, header-line form binding, and axis form bindings.
- Remove graph-only lifecycle-wrapper entries and graph DOM startup initialization.
- Keep all model commands, graph rendering, FFSX/Plotly parser/serializer/import/export functions, and Mantine-owned actions.

## Rationale

The application change is intentionally the same cutover as `003`. The failed patch used weak context before the large `#addGraphObject` deletion; this corrected patch anchors that block at its actual source location. No design rollback or legacy compatibility concession is introduced.

## Source of truth

Project chart state and `appFSM` remain authoritative. Mantine is the sole generic graph UI path. FFSX/Plotly functions remain UI-independent file compatibility boundaries.

## EFSM / core impact

None. Only obsolete DOM event entrypoints/startup installation are removed.

## File-format compatibility

No FFPX, FFSX, Plotly JSON or asset file format behavior changes.

## Validation

Repository CI must pass patch application, JavaScript syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert the application commit for this patch. `003` requires no rollback because it never applied.

## Follow-up

Delete the dead legacy graph form readers/renderers/helpers and then remove `#buildBox`, its required IDs, and graph-only CSS.
