# 20260914-017 — Remove legacy caption panel markup

## Purpose
Remove the native legacy caption-settings panel after Mantine ownership and legacy event detachment.

## Exact changes
- Delete `section#captionPanel` and its legacy controls from `Fast-figure.html`.
- Preserve `dashboardCaption`, `captionPrefix`, and `captionText` because they are the actual rendered figure caption surface.
- Preserve caption project state, EFSM events, export rendering, and Mantine controls.

## Rationale
The old popup panel no longer owns caption editing. The dashboard caption surface is separate from this settings panel and remains intact.

## Rejected alternatives
- Delete the dashboard caption together with the popup: rejected because it is rendered figure content, not legacy settings UI.
- Keep hidden legacy controls as a compatibility store: rejected because the Mantine/EFSM path is authoritative.

## Source of truth
Caption state remains in the existing project object/EFSM state.

## EFSM/core impact
None intended. Markup deletion only.

## Validation
Repository apply check, JS syntax, portable build, split and portable Chromium `file://` smoke tests.

## Rollback
Pre-deletion recovery commit: `264b01f539673821a239e7bb61e80ae0afb0abdf` (`Apply agent patch 20260914-016`).

## Follow-up
After success, verify the new apply commit and remove the legacy print panel.