# 20260914-010 — legacy sidebar and lifecycle startup cutover (count corrected)

## 목적
20260914-009가 첫 hunk의 unified-diff line count 오류로 적용되지 않았으므로 동일 cutover를 정확한 범위로 재적용한다.

## 변경
- `syncHeaderHeight()` 및 `installSidebarControls()` 제거.
- `connectLifecycleTasksToFSM()` 제거.
- startup에서 legacy header/popup/sidebar/help/layout-resizer/showSlotBorders/projectName DOM 초기화를 제거.
- core renderer/model 초기화와 `appFSM.ready()`는 유지.

## 이유
Mantine AppShell과 Mantine action lifecycle이 이미 해당 역할을 소유한다. old header/aside/layout markup을 물리적으로 삭제하기 전에 startup이 해당 DOM을 요구하지 않도록 해야 한다.

## Source of truth
sidebar는 Mantine UI state, lifecycle은 `appFSM`, project name은 project model이다.

## EFSM/core 영향
FFPX/FFSX/Plotly, project schema, renderer/domain 알고리즘은 변경하지 않는다.

## 검증
workflow 전체 검증을 통과해야 한다.

## 롤백
patch revert로 legacy sidebar/lifecycle startup wiring을 복원할 수 있다. persistent migration은 없다.

## WIP
legacy debug controls와 dead form helpers/HTML markup은 후속 cutover 대상이다.
