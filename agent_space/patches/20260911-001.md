# 20260911-001 — Remove dead legacy image-preview CSS

## Purpose

Remove the CSS that existed only for the deleted legacy `#imagePreview` element. The functional slot-image renderer remains unchanged.

## Exact changes

### `Fast-figure.html`
- Remove the `.image-preview` rule block.
- Keep `.slot-image-frame`, `.slot-image`, and `.slot-image.manual`, which belong to the dashboard renderer rather than the deleted sidebar form.

## Rationale

`20260910-037` removed the legacy image settings fieldset and `#imagePreview`. Keeping its CSS would preserve dead UI compatibility code with no functional consumer.

## Source of truth

Image display remains the dashboard slot renderer and Mantine image editor. No legacy sidebar image preview remains.

## EFSM / core impact

None.

## File-format compatibility

No FFPX, FFSX, Plotly JSON, image serialization, parser, serializer, or package version code is changed.

## Validation

Repository CI must pass patch application, JavaScript syntax, portable build, and split/portable Chromium `file://` smoke tests.

## Rollback

Revert the application commit for this patch. No stored data changes are involved.

## Follow-up

Remove the legacy `buildBox` graph-editor facade and its DOM form synchronization/readback path now that Mantine owns graph data, object, layout/axis, palette, and FFSX/Plotly file actions.
