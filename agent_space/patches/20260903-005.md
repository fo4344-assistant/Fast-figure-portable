# 20260903-005 — workspace EFSM entry 분리 재시도

## 상태

`20260903-004`는 `git apply --check`에서 `corrupt patch at ...:106`으로 실패했고 HTML에는 적용되지 않았다.

원인은 마지막 workspace hunk의 line count 오류다. 실제 hunk는 old/new `45/72`인데 `47/74`로 기록되어 있었다. 구현 내용과 context에는 문제가 확인되지 않았다.

## 목적

`004`와 동일하게 workspace EFSM entry의 domain synchronization과 legacy presentation을 분리한다.

## 변경 내용

- 앱 버전 `1.1.106-alpha` → `1.1.107-alpha`
- `updateFileAvailability(model)`
- `syncProjectWorkspaceDomainState()`
- `syncGraphWorkspaceDomainState()`
- `syncImageWorkspaceDomainState()`
- `exitGraphWorkspaceDomainState()`
- 기존 `sync*WorkspaceState()`는 domain 결과를 받아 legacy presentation 수행

## 004와의 차이

기능 설계 변경 없음. 마지막 unified diff hunk header를 실제 내용에 맞게 `45/72`로 교정한다.

## 검증

- `git apply --check`
- `git diff --check`
- application JavaScript `node --check`
- project/graph/image workspace의 기존 legacy 표시 의미 유지
- domain helper 단독 경로에 native form 접근이 없는지 확인

## rollback

`004` 설명과 동일하다.